# 安全性说明

## 设计前提

* 本程序是**纯本地应用**：没有开发者运营的服务器，不采集遥测，不上传使用数据。
* 用户凭证（账号、密码、API Key、LoTW 证书口令）只保存在本机凭证库：
  Windows 使用 DPAPI 加密（绑定当前用户），其他平台使用 ChaCha20 + HMAC-SHA256
  并由主口令解锁。**不存明文、不写入配置文件、不进日志与导出物。**
* 所有出站请求先经域名白名单校验（qslcard/privacy.py 的 DEFAULT_ALLOWED_HOSTS），
  非白名单域名在打开套接字之前即被拒绝；配置 offline: true 可彻底断网。
* LoTW 证书口令只通过子进程环境变量传给 TQSL，不出现在命令行参数中。
* 回写 QRZ Logbook 时只发送呼号 / 日期 / 波段 / 模式与 QSL 状态字段，
  不发送地址或联系方式。

## 报告问题

如果你发现凭证明文泄露、绕过白名单或越权访问远程数据的路径，
请通过 GitHub 的 Security Advisory（仓库 Security 标签页）私下报告，
不要直接开公开 issue。请在报告里给出复现步骤与受影响版本。
